# filebeat.modules:
# - module: elasticsearch
#   server:
#     enabled: true

# - module: logstash
#   log:
#     enabled: true

filebeat.inputs:
- type: container
  paths:
    - '/var/lib/docker/containers/*/*.log'

output.logstash:  
   hosts: ["localhost:5000"]
    